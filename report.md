# تقرير الحالة الشامل لمشروع SARH ERP
**SARH ERP — Comprehensive System Status & Architecture Report**

---

## 📌 1. نظرة عامة على النظام والرؤية المعمارية (Executive Overview)

نظام **SARH ERP** هو منصة سحابية متعددة المستأجرين (**Multi-tenant SaaS**) مخصصة بالكامل لقطاع **شركات المقاولات، التطوير، والتشطيبات والديكور**.

### 🔄 مسار دورة العمل التجارية في النظام (Business Pipeline Flow):
```text
  ┌────────────────┐
  │  1. Customers  │ (إدارة بيانات العملاء الأفراد والشركات)
  └───────┬────────┘
          ▼
  ┌────────────────┐
  │   2. Leads     │ (التقاط الطلبات السريعة وتأهيل مسار التواصل)
  └───────┬────────┘
          ▼
  ┌────────────────┐
  │ 3. Opportunity │ (إدارة الفرص البيعية، القيمة التقديرية، التفاوض)
  └───────┬────────┘
          ▼
 ┌─────────────────────────────────────────────────────────────┐
 │                       المراحل القادمة                        │
 │ Site Visit → Measurement → BOQ → Quotation → Contract ...   │
 └─────────────────────────────────────────────────────────────┘
```

---

## 🏗️ 2. البنية التحتية والطبقات التقنية (Core Architecture)

### 🔹 أ. معمارية تعدد المستأجرين (Multi-Tenancy Infrastructure):
- **عزل قواعد البيانات (Database-per-Tenant Isolation):** يتم إنشاء قاعدة بيانات منفصلة تماماً لكل مستأجر (`tenant`) عند التسجيل، مما يمنح أقصى درجات الأمان وعزل البيانات.
- **التعرف الديناميكي على المستأجر:** عبر الـ Subdomain، أو كود الشركة في الـ Path أو الـ Header (`X-Company-Code`).
- **نظام الترحيل والتهيئة الآلي (Automatic Provisioning):** تشغيل الـ Migrations والـ Seeders الخاصة بكل مستأجر برمجياً عند إنشاء الحساب.

### 🔹 ب. الحماية والأمان (Security & Hardening):
- **التحكم بالصلاحيات والأدوار (RBAC):** استخدام مكتبة `spatie/laravel-permission` مع تخصيصها للعمل مع الـ Multi-tenancy وعزل الصلاحيات لكل شركة.
- **الحماية من هجمات الـ IDOR:** منع أي مستخدم من الوصول إلى بيانات أو مستندات لا تخص مستأجره أو لا يمتلك إذناً عليها.
- **الحماية من الـ SQL Injection:** فحص وتطبيق القوائم البيضاء (`Whitelist Validation`) على جميع معاملات الفرز والترتيب (`sort_by`, `sort_order`).
- **الحماية من الـ DOS / Rate Limiting:** تحديد سقوف للأرقام والصفحات (`per_page` محدد بين 1 إلى 100).
- **سجل الأنشطة والتدقيق (Activity Log & Audit Trail):** تسجيل كافة الأحداث والعمليات التجارية والتغييرات على البيانات مع تسجيل المستخدم والوقت والحدث باللغة العربية الواضحة وبدون تعريض أي بيانات حساسة أو Payload غير ضروري.

### 🔹 ج. واجهة المستخدم والتجربة (Frontend & UX Conventions):
- **التقنيات المستخدمة:** Vue 3 (Composition API / `<script setup>`), Vite, TailwindCSS, Pinia, Vue-I18n, Lucide Icons.
- **نظام التصميم:** واجهة متناسقة مع خطوط عربية حديثة ودعم كامل للوضع الليلي والنهاري (`Dark / Light Mode`).
- **معالجة التواريخ:** عزل كامل للتوقيتات والتواريخ عبر مكتبة `date.js` لمنع ظهور الصيغ الخام غير المقروءة (`ISO timestamps`).
- **مكونات عامة قابلة لإعادة الاستخدام:**
  - `CrudIndex.vue`: جدول ديناميكي يدعم البحث، الفلترة، الترقيم، التصدير، وإدارة الحقول.
  - `CrudModal.vue`: نوافذ منبثقة تفاعلية للعمليات السريعة.
  - `InputMaker.vue`: توليد النماذج والمدخلات والتحقق منها بناءً على الـ Schema القادمة من الـ Backend.
  - `SearchableSelect.vue`: قوائم اختيار منسدلة ذكية تدعم البحث الحي.

---

## 🚀 3. تفاصيل المراحل المنفذة والمنجزة (Completed Phases)

### 📁 المرحلة 3: إدارة العملاء (Customer Management)
- **الهدف:** بناء السجل المركزي للعملاء الذين تتعامل معهم الشركة.
- **أهم الميزات:**
  - دعم نوعين من العملاء: أفراد (`individual`) وشركات (`company`).
  - حفظ وتدقيق بيانات الاتصال (الاسم، الشركة، الهاتف، الواتساب، البريد، الرقم الضريبي، السجل التجاري، العنوان).
  - إمكانية الإضافة السريعة للعملاء من داخل شاشات العمليات الأخرى (`Quick Customer Modal`).
  - إحصائيات فورية (إجمالي العملاء، الأفراد، الشركات، العملاء النشطين).

---

### 🎯 المرحلة 4: إدارة العملاء المحتملين (Lead Management)
- **الهدف:** سرعة التقاط استفسارات وطلبات العملاء الواردة من قنوات التسويق المختلفة قبل تحويلها إلى فرص بيعية رسمية.
- **أهم الميزات:**
  - ربط العميل المحتمل بعميل مسجل مسبقاً أو إنشاؤه عبر الـ Quick Modal.
  - دعم مصادر الوصول المتعددة: (فيسبوك، انستجرام، واتساب، موقع إلكتروني، إعلانات جوجل، إحالة، اتصال هاتفي، زيارة فرع).
  - **مسار حالات العميل المحتمل المبسط:**
    - جديد (`New`)
    - تم التواصل (`Contacted`)
    - مؤهل (`Qualified`)
    - غير مؤهل (`Unqualified`)
    - فرصة ضائعة (`Lost`)
    - تم التحويل (`Converted`)
  - **واجهة عرض التفاصيل السريعة:**
    - تم تبسيط نافذة العرض بحيث تعرض بيانات العميل وتفاصيل الطلب.
    - إمكانية تغيير وتحديث الحالة فوراً بنقرة زر بدون تعقيد أو مدخلات زائدة.
    - زر مباشر وواضح `[تحويل إلى فرصة]` ينقل العميل المحتمل للخطوة التالية.

---

### 💼 المرحلة 5: إدارة الفرص البيعية (Opportunity Management)
- **الهدف:** إدارة العمليات البيعية النشطة والمقايسات والقيم التقديرية والتفاوض حتى إغلاق الصفقة.
- **أهم الميزات:**
  - ربط الفرصة بالعميل (`Customer`)، وبالطلب المحتمل الأصلي (`Lead`) كمرجع تاريخي.
  - **مراحل الفرصة البيعية (Opportunity Stages):**
    - `New` (جديدة)
    - `Qualified` (مؤهلة للمقايسة)
    - `Proposal` (تقديم عرض السعر)
    - `Negotiation` (قيد التفاوض والمراجعة)
    - `Won` (تم الفوز والاعتماد)
    - `Lost` (فرصة ملغاة / مفقودة)
  - **بيانات الفرصة:**
    - عنوان الفرصة والوصف الفني والملاحظات الداخلية.
    - القيمة التقديرية المتوقعة للفرصة بالجنيه المصري (`estimated_value`).
    - تاريخ البدء المتوقع وتاريخ الإغلاق المتوقع (`expected_start_date`, `expected_close_date`).
    - إسناد الفرصة لمسؤول المبيعات / المهندس المختص (`assigned_to`).
  - **الأمان والنزاهة المالية:**
    - منع التحويل المزدوج للعميل المحتمل (`Duplicate Conversion Prevention`).
    - تنفيذ عمليات التحويل داخل معاملات ذرية آمنة (`DB::transaction`).
    - لوحة إحصائيات مالية مجمعة فورية عبر استعلامات SQL مباشرة فائقة السرعة.

---

## 📊 4. هيكل الجداول في قواعد البيانات (Database Schema Overview)

```text
┌─────────────────────────────────────────────────────────┐
│                    Tenant Database                      │
├─────────────────────────────────────────────────────────┤
│ • customers                                             │
│   - id, tenant_id, customer_type, name, company_name,   │
│     phone, whatsapp, email, tax_number, status, ...     │
├─────────────────────────────────────────────────────────┤
│ • leads                                                 │
│   - id, customer_id, title, description, source,        │
│     status, estimated_value, assigned_to, ...           │
├─────────────────────────────────────────────────────────┤
│ • opportunities                                         │
│   - id, customer_id, lead_id, title, description,       │
│     stage, estimated_value, expected_start_date,        │
│     expected_close_date, assigned_to, created_by, ...   │
├─────────────────────────────────────────────────────────┤
│ • activity_log                                          │
│   - id, log_name, description, subject_type,            │
│     subject_id, causer_id, properties, ...              │
└─────────────────────────────────────────────────────────┘
```

---

## 🧪 5. جودة الكود ونتائج الاختبارات المؤتمتة (QA & Test Verification)

تم بناء جناح اختبارات كامل ودقيق يغطي كافة جوانب الأمان، الوظائف، الصلاحيات، وعزل البيانات:

| ملف الاختبار | الهدف من الاختبار | الحالة |
|---|---|:---:|
| `TenantProvisioningTest.php` | اختبار إنشاء المستأجرين وترحيل قواعد بياناتهم | ✅ ناجح |
| `SecurityAttackVerificationTest.php` | اختبار صد هجمات IDOR, SQL Injection, DOS | ✅ ناجح |
| `CustomerManagementTest.php` | اختبار كافة عمليات إدارة العملاء والبحث والصلاحيات | ✅ ناجح |
| `LeadManagementTest.php` | اختبار التقاط وتحديث وتعيين وتحويل العملاء المحتملين | ✅ ناجح |
| `OpportunityManagementTest.php` | اختبار الفرص البيعية، المراحل، ومنع التحويل المزدوج | ✅ ناجح |

**نتيجة الاختبارات الإجمالية:**
- **66 اختبار مؤتمت (Feature Tests)**
- **413+ تأكيد دقيق (Assertions)**
- **نسبة النجاح:** **100% (0 أخطاء، 0 فشل)**.

---

## 🔮 6. الوضع الحالي والخطوات القادمة (Current State & Next Steps)

1. **الوضع الحالي:** كافة المراحل السابقة (الأمان، المستأجرون، العملاء، العملاء المحتملون، الفرص البيعية) مكتملة ومترابطة برمجياً وفي واجهات المستخدم ومختبرة بالكامل.
2. **الاستعداد للمرحلة القادمة:** النظام جاهز الآن للانتقال إلى المرحلة الفنية والهندسية:
   - **Phase 6:** إدارة المعاينات الميدانية (`Site Visits / معاينات الموقع`).
   - يليها: المقايسات الفنية (`Measurements`) ثم جداول الكميات (`BOQ`)، عروض الأسعار (`Quotations`)، والعقود (`Contracts`).
